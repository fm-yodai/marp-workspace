{
  "name": "marp-workspace",
  "version": "1.0.0",
  "description": "Multi-deck presentation workspace using Marp",
  "private": true,
  "workspaces": [
    "decks/*"
  ],
  "scripts": {
    "create-deck": "tsx scripts/create-deck.ts",
    "create-context": "tsx scripts/create-context.ts",
    "build:deck": "tsx scripts/build-deck.ts",
    "ocr:convert": "tsx scripts/ocr-convert-pptx.ts",
    "ocr:install": "node scripts/utils/install-python-deps.js",
    "ocr:verify": "tsx scripts/verify-ocr-setup.ts",
    "clean": "rimraf dist node_modules",
    "test:cross-platform": "tsx scripts/test-cross-platform.ts",
    "prepare": "node -e \"['dist/html','dist/pdf','dist/pptx','dist/pptx-editable'].forEach(d=>require('fs').mkdirSync(d,{recursive:true}))\""
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.0",
    "@types/inquirer": "^9.0.0",
    "@types/node": "^20.0.0",
    "commander": "^11.0.0",
    "cross-env": "^7.0.3",
    "gray-matter": "^4.0.3",
    "inquirer": "^9.2.0",
    "ora": "^8.0.0",
    "rimraf": "^5.0.10",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
